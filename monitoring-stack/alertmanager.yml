global:
  resolve_timeout: 5m

route:
  receiver: 'discord'
  group_wait: 5s
  group_interval: 5s
  repeat_interval: 1h

receivers:
  - name: 'discord'
    webhook_configs:
      - url: "http://discord-bridge:9094/webhook"
        send_resolved: true
